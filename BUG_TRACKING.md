# 缺陷追踪记录 | BUG TRACKING

**项目**: 10kV配电网单相接地故障定位系统  
**文件版本**: v1.0  
**记录方式**: Claude Code 发现并创建条目，Antigravity-Gemini 填写验证结果和推送记录

---

## 缺陷状态定义

| 状态标签 | 含义 |
|---------|------|
| `[待分析]` | 已记录现象，根因未明 |
| `[已定位]` | 根因已找到，修复方案待确认 |
| `[修复中]` | 代码正在修改 |
| `[待验证]` | 代码修改完成，等待测试 |
| `[已关闭]` | 验证通过，问题解决 |
| `[挂起]` | 需要更多信息或硬件配合 |

---

## 活跃缺陷列表

### BUG-001 · `[待分析]` · P0-致命

**现象描述**  
CAN模块通讯指示灯亮绿色（物理层正常），但主机CAN模块列表显示该模块离线。

**发现时间**: 待填写  
**影响范围**: 全部或部分CAN模块  
**复现概率**: 高（稳定复现）

**初步嫌疑方向**
- [ ] 节点ID冲突
- [ ] 帧类型不匹配（标准帧 vs 扩展帧）
- [ ] CAN过滤器配置错误（主机侧）
- [ ] 在线判断逻辑存在缺陷（超时参数/心跳帧匹配问题）

**根因分析**（Claude Code 填写）
```
待分析
```

**涉及文件**
```
待填写（格式：文件路径:行号）
```

**修复方案**（Claude Code 填写）
```
待填写
```

**验证方法**
```
待填写
```

**Antigravity验证记录**
- 验证时间：
- 验证结论：
- Git commit hash：

---

### BUG-002 · `[待分析]` · P1-严重

**现象描述**  
少数CAN模块（如2-3个）时通讯较为正常，模块数量增多后通讯异常（丢包、超时、离线等）。

**发现时间**: 待填写  
**影响范围**: 模块数量 > 阈值时（阈值未知，待测定）  
**复现概率**: 高（可通过增加模块数量复现）

**初步嫌疑方向**
- [ ] CAN总线负载过高（发送帧频率过密）
- [ ] 终端电阻配置缺失或错误（仅影响多节点时）
- [ ] 主机发送缓冲区（Mailbox）溢出无处理
- [ ] 错误帧风暴（某个模块异常导致总线连锁反应）
- [ ] RT-Thread CAN接收队列深度不足
- [ ] 光纤转CAN模块延迟在多节点时累积超限

**根因分析**（Claude Code 填写）
```
待分析
```

**涉及文件**
```
待填写
```

**修复方案**（Claude Code 填写）
```
待填写
```

**验证方法**
```
连接 3 / 5 / 8 / N 个模块分别测试，记录通讯正常的阈值
```

**Antigravity验证记录**
- 验证时间：
- 验证结论：
- Git commit hash：

---

### BUG-003 · `[待分析]` · P1-严重

**现象描述**  
配置信息由云端上位机下发至主机，通过CAN通讯显示模块在线/离线状态及回路配置。配置下发后，在线/离线状态显示异常。

**发现时间**: 待填写  
**影响范围**: 配置下发操作后  
**复现概率**: 待测（是否每次配置下发后均出现）

**初步嫌疑方向**
- [ ] 配置下发时重置了在线状态列表
- [ ] 云端配置的模块ID与实际CAN总线模块ID不一致
- [ ] 配置下发与CAN心跳接收存在竞争条件（Race Condition）
- [ ] 共享状态数据结构缺少互斥锁保护
- [ ] 屏幕刷新逻辑与底层状态数据不同步

**根因分析**（Claude Code 填写）
```
待分析
```

**涉及文件**
```
待填写
```

**修复方案**（Claude Code 填写）
```
待填写
```

**验证方法**
```
执行配置下发操作，在下发前后各记录一次模块在线状态，对比是否一致
```

**Antigravity验证记录**
- 验证时间：
- 验证结论：
- Git commit hash：

---

## 已关闭缺陷归档

> 此处记录已关闭的缺陷，保留历史可追溯性。

| BUG编号 | 简述 | 根因 | 关闭时间 | Commit |
|---------|------|------|---------|--------|
| —       | —    | —    | —       | —      |

---

## 新增缺陷模板

发现新问题时，复制以下模板并追加到"活跃缺陷列表"中：

```markdown
### BUG-XXX · `[待分析]` · P?-级别

**现象描述**  
（填写：在什么条件下，出现了什么现象）

**发现时间**: 
**影响范围**: 
**复现概率**: 

**初步嫌疑方向**
- [ ] 

**根因分析**（Claude Code 填写）

**涉及文件**

**修复方案**（Claude Code 填写）

**验证方法**

**Antigravity验证记录**
- 验证时间：
- 验证结论：
- Git commit hash：
```
